<!DOCTYPE html>
<html lang="en">
	<head>
		<title><%= title %></title>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1">
		<link href="./css/style.css" rel="stylesheet" type="text/css">
	</head>
	<body>
		<%- include('../partials/head', {title: title}); %>
		<div class="randomApp full-width">
			<% for(listing of info) { %>
				<div class="randomListing">
					<img src=<%= listing.picture %> width=150 class="clipped">
					<div class="randomText">
						<%= listing.name %>
					</div>
				</div><br>
			<% } %>
		</div>
		<%- include('../partials/footer'); %>
	</body>
</html>